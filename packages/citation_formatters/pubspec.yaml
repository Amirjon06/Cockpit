name: citation_formatters
description: Shared citation formatting for Cockpit writing modules.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.2

dev_dependencies:
  test: ^1.25.0
